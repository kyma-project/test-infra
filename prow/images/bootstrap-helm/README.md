# Bootstrap-helm Docker image

## Overview

This folder contains the Bootstrap-helm image that is based on the Bootstrap image.

The image consists of:

- helm

## Installation

To build the Docker image, run this command:

```bash
docker build bootstrap-helm .
```
