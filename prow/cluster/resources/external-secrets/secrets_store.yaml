# definition of the secret store, applicable to all clusters
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secretstore
  namespace: external-secrets
spec:
  provider:
    gcpsm:
      projectID: sap-kyma-prow
