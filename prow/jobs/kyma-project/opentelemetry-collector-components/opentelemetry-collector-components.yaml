# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
kyma-project/opentelemetry-collector-components:
    - name: pre-main-occ-otel-collector
      annotations:
        description: "Pre-submit job to build OpenTelemetry Collector image."
        owner: "huskies"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-main-tpi-otel-collector"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
      run_if_changed: '^otel-collector/'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240605-0b95104d"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=occ/otel-collector"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=otel-collector/Dockerfile"
              - "--build-arg='BUILD_MODE=PR'"
              - "--env-file=otel-collector/envs"
              - "--build-in-ado=true"
            env:
              - name: "ADO_PAT"
                valueFrom:
                  secretKeyRef:
                    name: "image-builder-ado-token"
                    key: "token"
            resources:
              requests:
                memory: 6Gi
                cpu: 1
              limits:
                memory: 6Gi
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config

postsubmits: # runs on main
  kyma-project/opentelemetry-collector-components:
    - name: post-occ-otel-collector-release
      annotations:
        description: "Post-submit job to build OpenTelemetry Collector image for a release"
        owner: "huskies"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-occ-otel-collector-release"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
        preset-signify-prod-secret: "true"
      always_run: true
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^\d+\.\d+\.\d+$
      reporter_config:
        slack:
          channel: huskies-notifications
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240605-0b95104d"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=occ/otel-collector"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=otel-collector/Dockerfile"
              - "--env-file=otel-collector/envs"
              - "--build-arg='BUILD_MODE=release'"
              - "--build-in-ado=true"
              - "--tag={{ .Env \"OTEL_VERSION\" }}-$(PULL_BASE_REF)
            env:
              - name: "ADO_PAT"
                valueFrom:
                  secretKeyRef:
                    name: "image-builder-ado-token"
                    key: "token"
            resources:
              requests:
                memory: 6Gi
                cpu: 1
              limits:
                memory: 6Gi
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
    - name: post-main-occ-otel-collector
      annotations:
        description: "Post-submit job to build OpenTelemetry Collector image."
        owner: "huskies"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-main-tpi-otel-collector"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
        preset-signify-prod-secret: "true"
      run_if_changed: '^otel-collector/'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      reporter_config:
        slack:
          channel: huskies-notifications
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240605-0b95104d"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=occ/otel-collector"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=otel-collector/Dockerfile"
              - "--build-arg='BUILD_MODE=PR'"
              - "--env-file=otel-collector/envs"
              - "--build-in-ado=true"
              - "--tag={{ .Env \"OTEL_VERSION\" }}-{{ .ENV \"KYMA_COMPONENTS_VERSION }}"
            env:
              - name: "ADO_PAT"
                valueFrom:
                  secretKeyRef:
                    name: "image-builder-ado-token"
                    key: "token"
            resources:
              requests:
                memory: 6Gi
                cpu: 1
              limits:
                memory: 6Gi
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
