# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/test-infra:
    - name: pre-main-test-infra-image-syncer-dry-run
      annotations:
        description: "Dry-run of sync images from external sources to Kyma owned registry"
        owner: "neighbors"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-main-test-infra-image-syncer-dry-run"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
      run_if_changed: '^(cmd/image-syncer/external-images.yaml)'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/test-infra/ko/image-syncer:v20240704-3f2fe2c5"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/ko-app/image-syncer"
            args:
              - "--dry-run"
              - "--debug"
              - "--images-file=cmd/image-syncer/external-images.yaml"
              - "--target-repo-auth-key=$(GOOGLE_APPLICATION_CREDENTIALS)"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
  
postsubmits: # runs on main
  kyma-project/test-infra:
    - name: post-main-test-infra-image-syncer-run
      annotations:
        description: "Sync images from external sources to Kyma owned registry"
        owner: "neighbors"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-main-test-infra-image-syncer-run"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
      run_if_changed: '^(cmd/image-syncer/external-images.yaml)'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/test-infra/ko/image-syncer:v20240704-3f2fe2c5"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/ko-app/image-syncer"
            args:
              - "--images-file=cmd/image-syncer/external-images.yaml"
              - "--target-repo-auth-key=$(GOOGLE_APPLICATION_CREDENTIALS)"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
  