# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/serverless:
    - name: pre-serverless-controller-build
      annotations:
        description: "function controller build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-serverless-controller-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-controller"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/manager/Dockerfile"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: pre-serverless-webhook-build
      annotations:
        description: "function webhook build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-serverless-webhook-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-webhook"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/webhook/Dockerfile"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: pre-serverless-jobinit-build
      annotations:
        description: "function jobinit build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-serverless-jobinit-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-build-init"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/jobinit/Dockerfile"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
  
postsubmits: # runs on main
  kyma-project/serverless:
    - name: post-serverless-controller-build
      annotations:
        description: "function controller build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-serverless-controller-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-controller"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/manager/Dockerfile"
              - "--tag=$(PULL_BASE_REF)"
              - "--tag=$(PULL_BASE_SHA)"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: post-serverless-webhook-build
      annotations:
        description: "function webhook build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-serverless-webhook-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-webhook"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/webhook/Dockerfile"
              - "--tag=$(PULL_BASE_REF)"
              - "--tag=$(PULL_BASE_SHA)"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: post-serverless-jobinit-build
      annotations:
        description: "function jobinit build job"
        owner: "otters"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-serverless-jobinit-build"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-image-builder-ado-token: "true"
        preset-sa-kyma-push-images: "true"
      skip_if_only_changed: '^docs/|^examples/'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - main
        - release-*
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/image-builder:v20240624-fbeadfda"
            securityContext:
              privileged: false
              seccompProfile:
                type: RuntimeDefault
              allowPrivilegeEscalation: false
            command:
              - "/image-builder"
            args:
              - "--name=function-build-init"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=components/serverless/deploy/jobinit/Dockerfile"
              - "--tag=$(PULL_BASE_REF)"
              - "--tag=$(PULL_BASE_SHA)"
              - "--build-in-ado=true"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
  