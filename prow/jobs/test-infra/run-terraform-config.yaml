# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/test-infra:
    - name: pull-secret-leaks-log-scanner-tf-plan
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pull-secret-leaks-log-scanner-tf-plan"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^configs/terraform/secrets-leaks-log-scanner'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/dev/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "/bin/sh"
            args:
              - "-c"
              - "> /bin/terraform -chdir=/home/prow/go/src/github.com/kyma-project/test-infra/configs/terraform/secrets-leaks-log-scanner init && /tfcmt/tfcmt -owner $REPO_OWNER -repo $REPO_NAME -pr $PULL_NUMBER plan -- terraform -chdir=/home/prow/go/src/github.com/kyma-project/test-infra/configs/terraform/secrets-leaks-log-scanner plan -input=false"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
    - name: test-pull-secret-leaks-log-scanner-tf-plan
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "test-pull-secret-leaks-log-scanner-tf-plan"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^configs/terraform/secrets-leaks-log-scanner'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/dev/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "sleep"
            args:
              - "1800"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
    - name: pull-hide-tf-github-comments
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pull-hide-tf-github-comments"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^configs/terraform'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "/ghcomment/github-comment"
            args:
              - "hide"
              - "--org"
              - "$(REPO_OWNER)"
              - "--repo"
              - "$(REPO_NAME)"
              - "-pr"
              - "$(PULL_NUMBER)"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  
postsubmits: # runs on main
  kyma-project/test-infra:
    - name: post-secret-leaks-log-scanner-tf-apply
      annotations:
        pipeline.trigger: "pr-merge"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-secret-leaks-log-scanner-tf-apply"
        prow.k8s.io/pubsub.topic: "prowjobs"
      run_if_changed: '^configs/terraform/secrets-leaks-log-scanner'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/dev/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "/tfcmt/tfcmt"
            args:
              - "-owner"
              - "$(REPO_OWNER)"
              - "-repo"
              - "$(REPO_NAME)"
              - "-pr"
              - "$(PULL_NUMBER)"
              - "-chdir=/home/prow/go/src/github.com/kyma-project/test-infra/configs/terraform/secrets-leaks-log-scanner"
              - "apply"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  