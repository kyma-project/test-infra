# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/test-infra:
    - name: pull-build-image-rotate-service-account
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pull-build-image-rotate-service-account"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
      run_if_changed: '^development/secrets-rotator/cloud-run/rotate-service-account'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      spec:
        containers:
          - image: "eu.gcr.io/sap-kyma-neighbors-dev/image-builder:v20221109-d1ac84d4b"
            command:
              - "/image-builder"
            args:
              - "--name=test-infra/rotate-service-account"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=development/secrets-rotator/cloud-run/rotate-service-account/Dockerfile"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: pull-secrets-rotator-tf-plan
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pull-secrets-rotator-tf-plan"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^development/secrets-rotator/terraform'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/dev/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "/bin/sh"
            args:
              - "-c"
              - "/bin/terraform -chdir=./development/secrets-rotator/terraform init -input=false && /tfcmt/tfcmt -owner $REPO_OWNER -repo $REPO_NAME -pr $PULL_NUMBER -sha1 $PULL_PULL_SHA plan -- terraform -chdir=./development/secrets-rotator/terraform plan -input=false -auto-approve"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  
postsubmits: # runs on main
  kyma-project/test-infra:
    - name: post-build-image-rotate-service-account
      annotations:
        pipeline.trigger: "pr-merge"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-build-image-rotate-service-account"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-sa-kyma-push-images: "true"
      run_if_changed: '^development/secrets-rotator/cloud-run/rotate-service-account'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      spec:
        containers:
          - image: "eu.gcr.io/sap-kyma-neighbors-dev/image-builder:v20221109-d1ac84d4b"
            command:
              - "/image-builder"
            args:
              - "--name=test-infra/rotate-service-account"
              - "--config=/config/kaniko-build-config.yaml"
              - "--context=."
              - "--dockerfile=development/secrets-rotator/cloud-run/rotate-service-account/Dockerfile"
            resources:
              requests:
                memory: 1.5Gi
                cpu: 1
            volumeMounts:
              - name: config
                mountPath: /config
                readOnly: true
              - name: signify-secret
                mountPath: /secret
                readOnly: true
        volumes:
          - name: config
            configMap:
              name: kaniko-build-config
          - name: signify-secret
            secret:
              secretName: signify-dev-secret
    - name: post-secrets-rotator-tf-apply
      annotations:
        pipeline.trigger: "pr-merge"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-secrets-rotator-tf-apply"
        prow.k8s.io/pubsub.topic: "prowjobs"
      run_if_changed: '^development/secrets-rotator/terraform'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/dev/test-infra/terraform:PR-6893"
            securityContext:
              privileged: false
            command:
              - "/bin/sh"
            args:
              - "-c"
              - "/bin/terraform -chdir=./development/secrets-rotator/terraform init -input=false && /tfcmt/tfcmt -owner $REPO_OWNER -repo $REPO_NAME -pr $PULL_NUMBER -sha1 $PULL_PULL_SHA apply -- terraform -chdir=./development/secrets-rotator/terraform apply -input=false -auto-approve"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  