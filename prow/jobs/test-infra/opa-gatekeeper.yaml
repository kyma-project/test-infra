# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/test-infra:
    - name: pull-opa-getekeeper-tekton-tf-plan
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pull-opa-getekeeper-tekton-tf-plan"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^opa|^prow/cluster/resources/gatekeeper-constraints|^tekton/deployments/gatekeeper-constraints'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        serviceAccountName: prow-terraform-executor
        automountServiceAccountToken: true
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/test-infra/terraform:v20230215-6bf03a31"
            automountServiceAccountToken: true
            securityContext:
              privileged: false
            command:
              - "/bin/sh"
            args:
              - "-c"
              - "/bin/terraform -chdir=./opa/terraform/environments/prod init -input=false && /tfcmt/tfcmt -owner $REPO_OWNER -repo $REPO_NAME -pr $PULL_NUMBER -sha $PULL_PULL_SHA plan -- terraform -chdir=./opa/terraform/environments/prod plan -input=false # comment cleanup here"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  
postsubmits: # runs on main
  kyma-project/test-infra:
    - name: post-opa-getekeeper-tekton-tf-apply
      annotations:
        pipeline.trigger: "pr-merge"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-opa-getekeeper-tekton-tf-apply"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^opa|^prow/cluster/resources/gatekeeper-constraints|^tekton/deployments/gatekeeper-constraints'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        serviceAccountName: prow-terraform-executor
        automountServiceAccountToken: true
        containers:
          - image: "europe-docker.pkg.dev/kyma-project/prod/test-infra/terraform:v20230215-6bf03a31"
            automountServiceAccountToken: true
            securityContext:
              privileged: false
            command:
              - "/bin/sh"
            args:
              - "-c"
              - "/bin/terraform -chdir=./opa/terraform/environments/prod init -input=false && /tfcmt/tfcmt -owner $REPO_OWNER -repo $REPO_NAME -sha $PULL_BASE_SHA apply -- terraform -chdir=./opa/terraform/environments/prod apply -input=false -auto-approve"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  