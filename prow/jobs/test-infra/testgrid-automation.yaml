# Code generated by rendertemplates. DO NOT EDIT.


presubmits: # runs on PRs
  kyma-project/test-infra:
    - name: pre-test-infra-validate-testgrid-config
      annotations:
        pipeline.trigger: "pr-submit"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "pre-test-infra-validate-testgrid-config"
        prow.k8s.io/pubsub.topic: "prowjobs"
      run_if_changed: '^(prow/jobs/.*\.yaml)|(prow/testgrid-default\.yaml)$'
      skip_report: false
      decorate: true
      cluster: untrusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "gcr.io/k8s-prow/transfigure:v20220202-1256a81e66"
            securityContext:
              privileged: false
            command:
              - "/transfigure.sh"
            args:
              - "test"
              - "prow/config.yaml"
              - "prow/jobs"
              - "prow/testgrid-default.yaml"
              - "kyma"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
  
postsubmits: # runs on main
  kyma-project/test-infra:
    - name: post-test-infra-upload-testgrid-config
      annotations:
        pipeline.trigger: "pr-merge"
        testgrid-create-test-group: "false"
      labels:
        prow.k8s.io/pubsub.project: "sap-kyma-prow"
        prow.k8s.io/pubsub.runID: "post-test-infra-upload-testgrid-config"
        prow.k8s.io/pubsub.topic: "prowjobs"
        preset-bot-github-token: "true"
      run_if_changed: '^(prow/jobs/.*\.yaml)|(prow/testgrid-default\.yaml)$'
      skip_report: false
      decorate: true
      cluster: trusted-workload
      max_concurrency: 10
      branches:
        - ^master$
        - ^main$
      spec:
        containers:
          - image: "gcr.io/k8s-prow/transfigure:v20220202-1256a81e66"
            securityContext:
              privileged: false
            command:
              - "/transfigure.sh"
            args:
              - "/etc/github-token/token"
              - "prow/config.yaml"
              - "prow/jobs"
              - "prow/testgrid-default.yaml"
              - "kyma"
            resources:
              requests:
                memory: 3Gi
                cpu: 2
            volumeMounts:
              - name: github-token
                mountPath: /etc/github-token
                readOnly: true
        tolerations:
          - key: dedicated
            operator: Equal
            value: high-cpu
            effect: NoSchedule
        nodeSelector:
            dedicated: "high-cpu"
        volumes:
          - name: github-token
            secret:
              secretName: kyma-bot-github-token
  