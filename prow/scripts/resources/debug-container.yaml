apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: oom-debug
  namespace: default
spec:
  selector:
    matchLabels:
      name: oom-debug
  template:
    metadata:
      labels:
        name: oom-debug
    spec:
      containers:
      - name: oom-debug
        securityContext:
          privileged: true
        image: eu.gcr.io/kyma-project/test-infra/pr/prow-tools:PR-3450
        imagePullPolicy: Always
        command:
          - "/prow-tools/dockeroomfinder"
        volumeMounts:
          - mountPath: /var/run
            name: docker-socket
          - mountPath: /run/containerd
            name: containerd-socket
        resources:
          requests:
            memory: "50Mi"
            cpu: "100m"
          limits:
            memory: "50Mi"
            cpu: "1"
      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run
            type: Directory
        - name: containerd-socket
          hostPath:
            path: /run/containerd
            type: DirectoryOrCreate
