presubmits:
  kyma-test-infra-dev/test-infra:
    - name: pre-test-presubmit
      cluster: workload-cluster
      decorate: true
      always_run: true
      spec:
        containers:
          - image: alpine
            command: ["/bin/echo"]
            args:
              - "HELLO WORLD"
