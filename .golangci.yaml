# https://golangci-lint.run/usage/configuration/

linters:
  disable-all: true
  enable:
    - deadcode
    - exportloopref
    - gosimple
    - govet
    - ineffassign
    - misspell
    - staticcheck
    # - revive # replaces golint
