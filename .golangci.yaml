# https://golangci-lint.run/usage/configuration/

run:
  timeout: 2m
  # skip test files
  tests: false
linters:
  disable-all: true
  enable:
    - deadcode
    - exportloopref
    - gosimple
    - govet
    - ineffassign
    - misspell
    - staticcheck
    # - revive # replaces golint
