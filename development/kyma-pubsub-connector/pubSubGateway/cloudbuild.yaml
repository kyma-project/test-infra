steps:
  - id: build-image
    name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - --tag=$_REPOSITORY/pubsub-gateway:$_TAG
      - .
    dir: .
  substitutions:
    _REPOSITORY: eu.gcr.io/kyma-project/test-infra
  images:
    - $_REPOSITORY/pubsub-gateway:$_TAG