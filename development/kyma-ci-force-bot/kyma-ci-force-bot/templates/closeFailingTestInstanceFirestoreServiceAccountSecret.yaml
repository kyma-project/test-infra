apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Namespace }}-{{ .Values.closeFailingInstance.name }}-{{ .Values.closeFailingInstance.serviceAccount }}
  namespace:  {{ .Release.Namespace }}
  labels:
    app: {{ .Values.appName | quote }}
    release: {{ .Release.Name | quote }}
data:
  key: {{ .Values.closeFailingInstance.base64SerivceAccountKey | quote }}
