apiVersion: v1
kind: Secret
metadata:
  name: {{ .Name}}-ghsecret
  namespace:  {{ .Namespace }}
  labels:
    app: {{ .CmpAppName }}
    release: {{ .Name}}
data:
  secret: {{ default "" .WebhookSecretValue | b64enc | quote }}