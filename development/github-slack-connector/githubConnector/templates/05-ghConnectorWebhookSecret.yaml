apiVersion: v1
kind: Secret
metadata:
  name: {{ .Name}}-ghsecret
  namespace:  {{ .Namespace }}
  labels:
    app: mp-{{ .AppName }}
    release: {{ .Name}}
data:
  secret: {{ default "" .WebhookSecretValue | b64enc | quote }}