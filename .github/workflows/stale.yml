name: Manage Stale Issues

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  stale:
    uses: kyma-project/test-infra/.github/workflows/reusable-stale.yml@dbefa5f119885ba93fe619ba03268d78435bb957 # main
