name: pull-gitleaks
on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]

env:
  GITLEAKS_VERSION: v8.18.2

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Fetch gitleaks ${{ env.GITLEAKS_VERSION }}
        run: curl -Lso gitleaks.tar.gz https://github.com/gitleaks/gitleaks/releases/download/${{ env.GITLEAKS_VERSION }}/gitleaks_8.18.2_linux_x64.tar.gz && tar -xvzf ./gitleaks.tar.gz
      - name: Run gitleaks
        run: ./gitleaks detect --log-opts=${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }} --verbose --redact